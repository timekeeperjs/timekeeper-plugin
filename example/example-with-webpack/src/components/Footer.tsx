import React from 'react';
import './Footer.css'; // Import the CSS file for styling

const Footer = () => {
  return (
    <footer className="footer">
      this footer is belong to webpack
    </footer>
  );
};

export default Footer;